---
topic: {topic}
domain: {domain}
tags: [{tags}]
status: active
spec_version: 1
---

# {Title}

> [!summary]
> {one-line essence of the note — what the reader gets}

{Body sections mirroring the card type — see references/card-spec.md.
Keep paragraphs ≤4 lines; prefer bullets and tables; put pitfalls in
`> [!warning]` callouts.}

## References

1. {[Title](url)} — {one-line annotation}

## Sources

- {source_ref} ({confidence})

---

## {Translation heading in target language, e.g. 中文翻译 — omit this whole section when language is "en"}

**{Translated title — bold line, NOT a # heading (avoid duplicate H1 in the outline)}**

> [!summary]
> {mirrors the summary callout}

### {Translated section headings use H3}

{Chinese prose: one paragraph = one physical line — never hard-wrap CJK text
mid-sentence, renderers turn the newline into a space and corrupt the text.}
